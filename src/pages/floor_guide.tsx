import { PageTop } from 'src/components/pages'

type InitialProps = {}
type Props = {} & InitialProps

const Index = (_: Props) => {
  /* TODO: 各ページの実装 */
  return <PageTop />
}

export default Index
