import type { Meta, StoryObj } from '@storybook/react'
import { MainVisual } from '.'

const meta: Meta<typeof MainVisual> = {
  component: MainVisual
}
export default meta

export const Default: StoryObj<typeof MainVisual> = {}
