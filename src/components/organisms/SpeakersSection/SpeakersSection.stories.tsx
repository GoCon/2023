import type { Meta, StoryObj } from '@storybook/react'
import { SpeakersSection } from '.'

const meta: Meta<typeof SpeakersSection> = {
  component: SpeakersSection
}
export default meta

export const Default: StoryObj<typeof SpeakersSection> = {}
